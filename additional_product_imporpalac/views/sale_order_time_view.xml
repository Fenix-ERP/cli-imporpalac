<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherited sale order tree view. -->
    <record id="view_order_tree" model="ir.ui.view">
        <field name="name">sale.order.view.tree.inherit.payment.status.in
            .sale
        </field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="before">
                <field name="elapsed_time" optional="show" widget="badge"
                       decoration-info="elapsed_time != 'Sin despacho' and elapsed_time != 'Sin despacho completado'"
                       decoration-muted="elapsed_time in ['Sin despacho', 'Sin despacho completado']"/>
                <field name="dispatcher_id" optional="show" widget="many2one_avatar_user"/>
            </xpath>
        </field>
    </record>
    <!-- Inherited sale order quotation tree view. -->
    <record id="view_quotation_tree_with_onboarding" model="ir.ui.view">
        <field name="name">sale.order.view.tree.inherit.payment.status
            .in.sale
        </field>
        <field name="model">sale.order</field>
        <field name="inherit_id"
               ref="sale.view_quotation_tree_with_onboarding"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="before">
              <field name="elapsed_time" optional="show" widget="badge"
                       decoration-info="elapsed_time != 'Sin despacho' and elapsed_time != 'Sin despacho completado'"
                       decoration-muted="elapsed_time in ['Sin despacho', 'Sin despacho completado']"/>
              <field name="dispatcher_id" optional="show" widget="many2one_avatar_user"/>
            </xpath>
        </field>
    </record>
</odoo>
