<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Inherited sale order tree view. -->
    <record id="view_order_tree" model="ir.ui.view">
        <field name="name">sale.order.view.tree.inherit.payment.status.in
            .sale
        </field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="before">
                <field
                    name="delivery_status"
                    widget="badge"
                    decoration-danger="delivery_status == 'waiting'"
                    decoration-warning="delivery_status == 'picking'"
                    decoration-muted="delivery_status == 'to_deliver'"
                    decoration-success="delivery_status == 'delivered'"
                />
                <field name="elapsed_time" optional="show" widget="badge" />
                <field name="picking_elapsed_time" optional="hide" widget="badge" />
                <field
                    name="dispatcher_id"
                    optional="show"
                    widget="many2one_avatar_user"
                />
            </xpath>
        </field>
    </record>
    <!-- Inherited sale order quotation tree view. -->
    <record id="view_quotation_tree_with_onboarding" model="ir.ui.view">
        <field name="name">sale.order.view.tree.inherit.payment.status
            .in.sale
        </field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_quotation_tree_with_onboarding" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='partner_id']" position="before">
                <field
                    name="delivery_status"
                    widget="badge"
                    decoration-danger="delivery_status == 'waiting'"
                    decoration-warning="delivery_status == 'picking'"
                    decoration-muted="delivery_status == 'to_deliver'"
                    decoration-success="delivery_status == 'delivered'"
                />
                <field name="elapsed_time" optional="show" widget="badge" />
                <field name="picking_elapsed_time" optional="hide" widget="badge" />
                <field
                    name="dispatcher_id"
                    optional="show"
                    widget="many2one_avatar_user"
                />
            </xpath>
        </field>
    </record>
</odoo>
